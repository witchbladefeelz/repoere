# Архитектура системы лицензий

## Схема работы

```
┌─────────────────┐
│  Telegram Bot   │
│    (bot.py)     │
└────────┬────────┘
         │
         │ 1. Создает ключ
         │    /purchase → генерирует KEY
         ▼
┌─────────────────┐
│  MySQL Database  │
│  subscriptions   │ ← Ключ сохраняется здесь
│  users           │ ← После активации создается запись
└────────┬────────┘
         │
         │ 2. Клиент активирует
         │    activate-license.php?hwid=XXX&key=YYY
         ▼
┌─────────────────┐
│   PHP API       │
│ activate-license│ → Проверяет ключ
│ check-license   │ → Проверяет статус
└────────┬────────┘
         │
         │ 3. Уведомление в бот
         │    send_telegram_message()
         ▼
┌─────────────────┐
│  Telegram Bot   │
│  (уведомление)  │
└─────────────────┘
         │
         │ 4. Клиент проверяет
         │    check-license.php?hwid=XXX
         ▼
┌─────────────────┐
│ Client App      │
│ (ваше приложение)│
└─────────────────┘
```

## Поток данных

### Активация лицензии

1. **Пользователь в боте:**
   - Нажимает "💳 Purchase Subscription"
   - Выбирает длительность (30/90/180/Lifetime)
   - Получает ключ: `KEYRCBFF06QOBT6F72V`

2. **Клиентское приложение:**
   ```
   GET activate-license.php?hwid=CLIENT_HWID&key=KEYRCBFF06QOBT6F72V
   ```

3. **PHP обрабатывает:**
   - Проверяет ключ в `subscriptions`
   - Создает/обновляет запись в `users`
   - Удаляет использованный ключ
   - Отправляет уведомление в бот
   - Возвращает результат клиенту

4. **Пользователь получает:**
   - Уведомление в Telegram бот
   - Подтверждение в клиентском приложении

### Проверка лицензии

1. **Клиентское приложение (при запуске/периодически):**
   ```
   GET check-license.php?hwid=CLIENT_HWID
   ```

2. **PHP обрабатывает:**
   - Ищет HWID в `users`
   - Проверяет срок действия
   - Проверяет статус бана
   - Возвращает результат

3. **Клиент получает:**
   - `valid: true/false`
   - `days_remaining`
   - `expired`, `banned` статусы

## Компоненты системы

### 1. Telegram Bot (bot.py)
- **Роль:** Генерация ключей, управление пользователями
- **Функции:**
  - Создание ключей подписки
  - Просмотр профиля пользователя
  - Админ-панель (бан, разбан, статистика)

### 2. PHP API
- **activate-license.php:** Активация ключей
- **check-license.php:** Проверка статуса лицензии
- **Роль:** Мост между ботом и клиентом

### 3. MySQL Database
- **subscriptions:** Хранит неиспользованные ключи
- **users:** Хранит активированные подписки с HWID

### 4. Клиентское приложение
- Получает ключ от пользователя
- Активирует через PHP API
- Периодически проверяет статус

## Безопасность

1. **Одноразовые ключи** - каждый ключ используется только один раз
2. **Привязка HWID** - один HWID может быть только у одного аккаунта
3. **Проверка бана** - забаненные пользователи не могут активировать
4. **Транзакции** - все операции атомарны (rollback при ошибке)

## Интеграция

PHP файлы полностью готовы к использованию. Они:
- ✅ Подключены к той же БД, что и бот
- ✅ Отправляют уведомления в бот при активации
- ✅ Обрабатывают все ошибки
- ✅ Поддерживают lifetime подписки
- ✅ Защищены от дублирования HWID

## Настройка для продакшена

1. **Измените пароль БД** в PHP файлах
2. **Используйте HTTPS** для защиты данных
3. **Настройте CORS** если нужно
4. **Добавьте rate limiting** для защиты от DDoS
5. **Настройте логирование** всех запросов

